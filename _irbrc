[
  -> { Readline.emacs_editing_mode },
  -> { require 'rubygems' },
  -> { require 'ruremai' }
].each do |proc|
  begin
    proc.call
  rescue Exception
    puts %([irbrc] #{$!.class} #{$!.message})
  end
end
