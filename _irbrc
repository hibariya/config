[
  -> { Readline.emacs_editing_mode },
  -> { require 'rubygems' },
  -> { require 'ruremai' },
  -> { require 'wirble'
    Wirble.init
    Wirble.colorize
  }
].each do |proc|
  begin
    proc.call
  rescue Exception
    puts %([irbrc] #{$!.class} #{$!.message})
  end
end
